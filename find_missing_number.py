# first we sort the array in log(n) by swapping each element using
# the realtion to the index and the value i.e. the index should have an i == n
# relationship

# after we sort the array we then iterate and any n that is != to i, we return i as the missing
# number

def find_missing_number(nums):
    return nums


print(find_missing_number([0, 4, 3, 1, 5]))
